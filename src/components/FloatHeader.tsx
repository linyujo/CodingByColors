import React from "react"
import styled from "styled-components"

const Header = styled.div``

interface Props {}

const FloatHeader: React.FC<Props> = () => {
  return <Header></Header>
}

export default FloatHeader
